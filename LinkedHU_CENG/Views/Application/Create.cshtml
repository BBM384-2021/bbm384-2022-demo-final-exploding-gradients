@model LinkedHU_CENG.Models.Application

@{
    ViewData["Title"] = "Create";
}

<form asp-controller="Application" asp-action="Create" method="post">
    <div class="border p-3 mt-4 custom-margin1 custom-shadow1">
        <div class="mb-3 ">
            <h3 class="text-primary">Apply to application.AdvertisementTitle at application.Company</h3>
        </div>
        <center><input type="button" value="Submit Application" onclick="@("window.location.href='" + @Url.Action("Index", "Advertisement") + "'");" /></center>
    </div>
</form>
